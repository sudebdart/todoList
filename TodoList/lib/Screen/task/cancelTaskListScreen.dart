import 'package:flutter/material.dart';

class cancelTastListScreen extends StatelessWidget {
  const cancelTastListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
